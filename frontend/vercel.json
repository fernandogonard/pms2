{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/((?!static|icons|manifest.json|favicon.ico|logo).*)",
      "destination": "/index.html"
    }
  ]
}
